import { Container, Typography } from '@material-ui/core';
import React from 'react';

const Footer = () => {
  return (
    <div>
      <Container></Container>
    </div>
  );
};

export default Footer;
